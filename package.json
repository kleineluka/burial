{
  "name": "burial",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "npm install && cd src-backend && cargo build && cd .. && npm run tauri dev",
    "test": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1"
  },
  "dependencies": { }
}
